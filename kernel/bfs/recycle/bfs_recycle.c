#include "../include/bfs_api.h"

int32_t BFS_MoveToRecycle(const char* path) {
    if (!path) return -1;
    return 0;
}

int32_t BFS_Restore(const char* recycle_id) {
    if (!recycle_id) return -1;
    return 0;
}
