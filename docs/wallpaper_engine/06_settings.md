# Settings Integration

A new tab has been added to the Settings application (pps.c). It uses wallpaper_settings_render() to display buttons for each registered wallpaper. Clicks trigger wallpaper_set().