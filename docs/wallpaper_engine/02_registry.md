# Wallpaper Registry

The registry maintains a static list of available wallpapers (Nature, Galaxy, Water, Mountains, Minimal) and caches their loaded BOSSurface instances to prevent redundant file I/O and decoding overhead.