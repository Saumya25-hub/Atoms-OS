# Wallpaper Engine

The Wallpaper Engine integrates BOPAWN with the Desktop Shell. By calling wallpaper_set(path), BOPAWN detects the format, decodes the image, caches it, and automatically refreshes the desktop background.