# BOPAWN Image Engine V2 Overview

BOPAWN is the native ATOMS OS image subsystem. It is a universal hybrid image engine designed to take any supported image format (PNG, BMP, RAW, ICO) and transform it into a common internal representation (BOSSurface).

It is the single image pipeline used by all GUI components in ATOMS OS.