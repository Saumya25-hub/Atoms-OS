# ATRIX Browser Engine v1.0 — CSS Architecture

## Style & Layout Specifications

- Rule Storage: `CSSStyleSheet` container with up to 32 rule bindings.
- Box Model: Margin, Border, Padding, and Content area bounds calculation.
- Color Parser: ARGB conversion from CSS names (`white`, `black`, `#1E1E2E`).
