# Launcher

The Desktop Launcher (`desktop_launch_application`) is a thin wrapper over the `app_registry`.

It standardizes the execution flow:
Desktop Icon / Start Menu -> Launcher -> Registry Callback -> Window Creation.
