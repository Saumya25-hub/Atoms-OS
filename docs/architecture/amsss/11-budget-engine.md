# Budget Engine

The budget defaults to the 256 MiB heap ceiling and may be reduced through the public API. Budget rejection triggers reclaim before allocation. It is not a process quota or security boundary.

