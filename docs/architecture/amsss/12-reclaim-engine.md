# Reclaim Engine

Up to 16 static providers are visited in registration order until the target is reached. Results are capped to the remaining target. Providers must only release unused resources and must not retain AMSSS internal pointers.

