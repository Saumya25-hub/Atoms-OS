#include "../include/gdi32_api.h"

bool BeginPath(HDC hdc) {
    (void)hdc;
    return (hdc != 0);
}

bool EndPath(HDC hdc) {
    (void)hdc;
    return (hdc != 0);
}

bool StrokeAndFillPath(HDC hdc) {
    (void)hdc;
    return (hdc != 0);
}
